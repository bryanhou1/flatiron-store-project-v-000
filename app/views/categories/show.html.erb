<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %></li>
    <li><%= number_to_currency(item.price/100.0) %></li>
<% end %>
</ul>